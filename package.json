{
  "name": "agentinfra",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "dotenv -e .env -- turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "test": "dotenv -e .env -- vitest run && turbo run test",
    "db:generate": "pnpm --filter @agentinfra/db generate",
    "db:migrate": "dotenv -e .env -- pnpm --filter @agentinfra/db migrate",
    "jobs": "dotenv -e .env -- tsx apps/jobs/src/local.ts",
    "jobs:status": "dotenv -e .env -- tsx scripts/job-status.ts",
    "cf:build:web": "node scripts/cloudflare-build.mjs web",
    "cf:build:jobs": "node scripts/cloudflare-build.mjs jobs",
    "cf:deploy:web": "node scripts/cloudflare-deploy.mjs web",
    "cf:deploy:jobs": "node scripts/cloudflare-deploy.mjs jobs",
    "changeset": "changeset",
    "version-packages": "changeset version && node scripts/sync-python-version.mjs && uv lock --project sdks/python && pnpm install --lockfile-only",
    "release:check": "node --test scripts/release-tools.test.mjs && node scripts/sync-python-version.mjs --check && uv lock --project sdks/python --check && turbo run typecheck build --filter=@papers.bot/sdk --filter=papers-bot --filter=@agentinfra/cli && vitest run packages/sdk-typescript/tests packages/cli/tests && turbo run test --filter=papers-bot && node scripts/package-install-check.mjs && python3 scripts/python-package-check.py",
    "publish-packages": "pnpm release:check && node scripts/publish-packages.mjs",
    "publish:npm": "pnpm release:check && node scripts/publish-packages.mjs npm",
    "publish:python": "pnpm release:check && node scripts/publish-packages.mjs python",
    "docs:openapi": "tsx scripts/docs-openapi.ts"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@playwright/test": "latest",
    "@types/node": "latest",
    "dotenv-cli": "latest",
    "prettier": "^3.9.6",
    "tsup": "^8.5.1",
    "tsx": "latest",
    "turbo": "latest",
    "typescript": "latest",
    "vitest": "latest",
    "wrangler": "4.132.0"
  },
  "type": "module"
}
